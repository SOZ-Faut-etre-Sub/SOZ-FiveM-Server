import React from 'react';

import { AppIcon } from '../../components/system/AppIcon';

const DialerIcon: React.FC = props => {
    return <AppIcon {...props} name="dialer" />;
};

export default DialerIcon;
