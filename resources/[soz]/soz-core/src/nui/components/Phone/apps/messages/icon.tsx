import React from 'react';

import { AppIcon } from '../../components/system/AppIcon';

const MessagesIcon: React.FC = props => {
    return <AppIcon {...props} name="messages" />;
};

export default MessagesIcon;
