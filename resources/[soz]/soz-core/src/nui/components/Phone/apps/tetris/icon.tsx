import React from 'react';

import { AppSimpleIcon } from '../../components/system/AppIcon';

const TetrisIcon: React.FC = props => {
    return <AppSimpleIcon {...props} name="tetris" />;
};

export default TetrisIcon;
