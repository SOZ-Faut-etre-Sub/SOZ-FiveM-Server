import React from 'react';

import { AppIcon } from '../../components/system/AppIcon';

const NotesIcon: React.FC = props => {
    return <AppIcon {...props} name="notes" />;
};

export default NotesIcon;
