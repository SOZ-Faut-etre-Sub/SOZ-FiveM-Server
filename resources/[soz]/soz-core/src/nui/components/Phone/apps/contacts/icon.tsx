import React from 'react';

import { AppIcon } from '../../components/system/AppIcon';

const ContactsIcon: React.FC = props => {
    return <AppIcon {...props} name="contacts" />;
};

export default ContactsIcon;
