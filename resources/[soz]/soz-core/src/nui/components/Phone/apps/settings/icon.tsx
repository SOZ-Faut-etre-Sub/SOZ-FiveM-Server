import React, { FunctionComponent } from 'react';

import { AppIcon } from '../../components/system/AppIcon';

const SettingsIcon: FunctionComponent<any> = props => {
    return <AppIcon {...props} name="settings" />;
};

export default SettingsIcon;
