import React, { FunctionComponent } from 'react';

import { AppIcon } from '../../components/system/AppIcon';

const SocietyContactsIcon: FunctionComponent<any> = props => {
    return <AppIcon {...props} name="society-contacts" />;
};

export default SocietyContactsIcon;
