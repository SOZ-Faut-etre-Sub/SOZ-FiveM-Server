import React from 'react';

import { AppIcon } from '../../components/system/AppIcon';

const CameraIcon: React.FC = props => {
    return <AppIcon {...props} name="camera" />;
};

export default CameraIcon;
