import React from 'react';

import { AppIcon } from '../../components/system/AppIcon';

const PhotosIcon: React.FC = props => {
    return <AppIcon {...props} name="photos" />;
};

export default PhotosIcon;
