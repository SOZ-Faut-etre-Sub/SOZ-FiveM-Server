import React from 'react';

import { AppSimpleIcon } from '../../components/system/AppIcon';

const SnakeIcon: React.FC = props => {
    return <AppSimpleIcon {...props} name="snake" />;
};

export default SnakeIcon;
