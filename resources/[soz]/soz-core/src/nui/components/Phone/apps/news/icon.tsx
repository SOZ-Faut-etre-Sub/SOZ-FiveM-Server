import React from 'react';

import { AppIcon } from '../../components/system/AppIcon';

const NewsIcon: React.FC = props => {
    return <AppIcon {...props} name="news" />;
};

export default NewsIcon;
