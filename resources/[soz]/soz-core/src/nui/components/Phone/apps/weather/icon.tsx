import React from 'react';

import { AppIcon } from '../../components/system/AppIcon';

const WeatherIcon: React.FC = props => {
    return <AppIcon {...props} name="weather" />;
};

export default WeatherIcon;
