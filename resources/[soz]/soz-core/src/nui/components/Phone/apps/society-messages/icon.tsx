import React, { FunctionComponent } from 'react';

import { AppIcon } from '../../components/system/AppIcon';

const SocietyMessagesIcon: FunctionComponent<any> = props => {
    return <AppIcon {...props} name="society-messages" />;
};

export default SocietyMessagesIcon;
